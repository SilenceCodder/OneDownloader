import 'package:flutter/material.dart';

class CategoryModel{
  String image, name,webUrl;
  Color color, iconColor;
  bool isImage;
  IconData icon;
  CategoryModel({this.image, this.name, this.color, this.icon, this.webUrl, this.iconColor, this.isImage});
}


